<form method="post" action="{{ path('admin_outsiders_delete', {'id': outsider.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ outsider.id) }}">
    <button type="submit" class="inline-block rounded bg-red-500 px-4 py-2 text-sm font-semibold text-white hover:bg-red-600">
        Supprimer
    </button>
</form>