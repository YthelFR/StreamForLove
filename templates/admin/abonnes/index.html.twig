{% extends 'base.html.twig' %}

{% block title %}Abonnes index{% endblock %}

{% block body %}
    <h1>Abonnes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonne in abonnes %}
            <tr>
                <td>{{ abonne.id }}</td>
                <td>{{ abonne.email }}</td>
                <td>
                    <a href="{{ path('app_abonnes_show', {'id': abonne.id}) }}">show</a>
                    <a href="{{ path('app_abonnes_edit', {'id': abonne.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_abonnes_new') }}">Create new</a>
{% endblock %}
