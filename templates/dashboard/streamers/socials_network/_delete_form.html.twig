<form method="post" action="{{ path('app_socials_network_delete', {'id': socials_network.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socials_network.id) }}">
	<button class="btn">Delete</button>
</form>
