<form method="post" action="{{ path('app_cagnotte_delete', {'id': cagnotte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cagnotte.id) }}">
    <button class="btn">Delete</button>
</form>
