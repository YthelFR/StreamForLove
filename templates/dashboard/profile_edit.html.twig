{% extends 'user_base.html.twig' %}

{% block body %}
	<div class="ml-auto mb-6 lg:w-[75%] xl:w-[80%] 2xl:w-[85%]">
		<div class="container mx-auto px-4 py-8">
			<h1 class="text-center text-2xl font-bold mb-4 text-gray-800">Modifier mon profil</h1>

			{% for label, messages in app.flashes %}
				<div class="bg-green-500 text-white p-3 rounded mb-4">
					{% for message in messages %}
						<p>{{ message }}</p>
					{% endfor %}
				</div>
			{% endfor %}

			<div class="mx-auto w-11/12 max-w-3xl border border-gray-300 p-5 shadow-lg rounded-lg bg-white">
				{{ form_start(form) }}

				<div class="mb-4">
					{{ form_label(form.email, null, {'attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
					{{ form_widget(form.email, {'attr': {'class': 'w-full border border-gray-300 p-2 rounded-lg'}}) }}
					{{ form_errors(form.email) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.pseudo, null, {'attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
					{{ form_widget(form.pseudo, {'attr': {'class': 'w-full border border-gray-300 p-2 rounded-lg'}}) }}
					{{ form_errors(form.pseudo) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.old_password, null, {'attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
					{{ form_widget(form.old_password, {'attr': {'class': 'w-full border border-gray-300 p-2 rounded-lg'}}) }}
					{{ form_errors(form.old_password) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.new_password.first, null, {'attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
					{{ form_widget(form.new_password.first, {'attr': {'class': 'w-full border border-gray-300 p-2 rounded-lg'}}) }}
					{{ form_errors(form.new_password.first) }}
				</div>

				<div class="mb-4">
					{{ form_label(form.new_password.second, null, {'attr': {'class': 'block text-gray-700 font-bold mb-2'}}) }}
					{{ form_widget(form.new_password.second, {'attr': {'class': 'w-full border border-gray-300 p-2 rounded-lg'}}) }}
					{{ form_errors(form.new_password.second) }}
				</div>

				<div class="flex justify-end mt-4">
					<button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Enregistrer</button>
				</div>

				{{ form_end(form) }}
			</div>
		</div>
	</div>
{% endblock %}
