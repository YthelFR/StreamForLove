{% extends 'base.html.twig' %}

{% block title %}Edit Presentation{% endblock %}

{% block body %}
    <h1>Edit Presentation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('streamer_presentations_index') }}">back to list</a>
{% endblock %}
